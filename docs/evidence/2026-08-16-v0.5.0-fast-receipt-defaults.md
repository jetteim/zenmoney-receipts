# Fast receipt defaults verification — 2026-08-16

## Scope and privacy

This evidence covers `F-018`: optional receipt date/account inputs, marked host-local date suggestions, bounded account recommendation, and preservation of the exact preview/confirmation boundary. It contains no credentials, account/category names or IDs, transactions, amounts, receipt content, balances, or raw ZenMoney responses.

## Offline verification

- `npm run check`: passed on Node.js 22.22.0.
- Tests: 12 files and 43 tests passed.
- TypeScript typecheck and production bundle: passed.
- MCP smoke: 22 bounded tools discovered; private backend process smoke passed.
- Repository validation: 15 required files checked with zero failures.
- Receipt-default coverage: host-local date resolution, semantic account hints, bounded payee history, deterministic low-confidence fallback, hostile/conflicting hint rejection before backend reads, no-write preview, exact suggested-field projection, confirmed application, and MCP optional-field schemas.
- Skill validation: `categorize-zenmoney-receipts` passed the system skill validator; the guarded refresh completed and the installed `SKILL.md` matched the repository copy byte-for-byte.
- Production dependency audit: zero vulnerabilities.

## Live read-only verification

`npm run doctor:live` passed for version 0.5.0. The credential source was reported as macOS Keychain, the registered Codex MCP pointed to this build, and synchronization returned 81 active categories. No credentials or financial records were printed and no data was changed.

## Write boundary and remaining evidence

No live write was run. Missing-field selection is computed during a side-effect-free preview and the resulting date/account ID is bound into the existing exact confirmation token. Existing live receipt-creation evidence already covers the downstream confirmed create/read-back path. A future user-confirmed receipt can exercise the combined suggestion-and-create experience naturally; this release does not require a synthetic financial mutation.
