# Version 0.3.0 verification — 2026-08-15

This record is sanitized. It contains no credential values, category names, transaction details, balances, receipt data, or raw ZenMoney responses.

## Offline release gate

Command: `npm run check`

Result:

```text
9 test files passed
25 tests passed
TypeScript check passed
Production bundles built: CLI, MCP server, private backend, backend entry
MCP stdio smoke passed: 16 tools
Private backend child-process smoke passed
Repository validation passed: 14 required durable files
```

## Manifest and skill validation

```text
Codex plugin validation passed
find-zenmoney-savings skill validation passed
MCP schema discovery returned version 0.3.0 and 16 tools
```

## Live read-only verification

Commands: `npm run test:live-readonly` and `npm run doctor:live`, run with macOS Keychain access.

```text
Credential source: macOS Keychain
ZenMoney synchronization: passed
Active category count: 79
Financial records printed: no
Writes requested: no
Codex registration: points to this build
```

A separate fresh ephemeral Codex session, running read-only and without saved chat history, called the registered MCP directly:

```text
zenmoney_connection_status: completed
zenmoney_sync(full=false): completed
configured: true
credential source: macOS Keychain
synchronization: succeeded
Categories/accounts/transactions/balances printed: no
Writes requested: no
```

The new savings evidence path was also exercised over the preceding 90 days:

```text
Spending-insights MCP call: passed
Transactions examined: 241
Expense transactions: 232
Instrument groups: 1
Truncated: no
Amounts, payees, category names, transaction IDs, credentials printed: no
Writes requested: no
```

## Private ChatGPT transport

```text
Official OpenAI tunnel-client: v0.0.11 installed
Release archive SHA-256 verification: passed
Local plan tooling: passed
Tunnel profile doctor: blocked because profile has not been initialized
Missing external prerequisites: organization tunnel ID/runtime key, workspace association, ChatGPT UI connection/tool discovery
```

Repository tooling is verified; this is not evidence that ChatGPT can yet see the connector.

The later laptop-hosted ChatGPT tunnel attempt was abandoned by user choice. Its stopped local runtime/profile/log artifacts were removed; the remote OpenAI tunnel remains account-side until manually deleted.

## Live synthetic write E2E

The explicitly authorized v0.3.0 synthetic suite ran against the final 16-tool build:

```text
Preflight: credential, synchronization, writable account, two active categories passed
New-receipt preview: 0.01 + 0.02 = 0.03; no preview-time write
New-receipt apply: exact total and repeat-apply idempotency verified
Reconciliation preview: 0.06 → 0.02 + 0.03 = 0.05; no preview-time write
Reconciliation apply: amount, split, categories, total, idempotency verified
Cleanup: 4/4 exact generated transaction IDs absent or deleted
Pre-existing transactions changed: none
```

The earlier detailed sanitized log remains at `docs/e2e-test-log-2026-08-15.md`.
