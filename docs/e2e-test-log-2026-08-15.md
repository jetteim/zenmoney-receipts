# Live E2E test log — 2026-08-15

## Scope

- Target: the configured private ZenMoney account.
- Write blast radius: uniquely labeled synthetic expenses worth a few cents, using connector-generated transaction IDs only.
- Protected data: no pre-existing transaction ID was supplied to a mutation.
- Rollback/cleanup: delete every generated ID in `finally`, then synchronize and verify each is absent or soft-deleted.
- Output sanitation: account names, category names, transaction IDs, and credentials are omitted from this log.

## Local verification

```text
npm run check
Test Files  8 passed (8)
Tests       21 passed (21)
MCP smoke test passed (15 tools).
Private backend process smoke test passed.

npm audit --omit=dev
found 0 vulnerabilities

Plugin validation passed
Skill is valid
```

## Live read-only preflight

```text
Live read-only test passed: Keychain credential, ZenMoney sync, 79 active categories. No writes requested.
```

## First write attempt and recovery

The first create request exposed an upstream payload gap and was rejected by ZenMoney with HTTP 400. The operation's compensating rollback and the harness cleanup both completed; the two preselected synthetic IDs were verified absent/deleted. No real transaction was touched.

```text
E2E scope: uniquely labeled synthetic expenses only; no pre-existing transaction will be changed.
Preflight passed: credential, sync, writable account, and two active categories.
New-receipt preview passed: 0.01 + 0.02 = 0.03 and no write occurred.
Error: ZenMoney API request failed with status 400; compensating rollback completed
Cleanup passed: 2/2 exact synthetic transaction IDs are absent or deleted.
```

Root cause: the pinned backend's generic transaction-create builder omitted four fields present in the complete live transaction schema. The connector was updated to use a private, complete create payload while retaining bounded public tools and concurrency-checked update/delete behavior.

## Successful live write E2E

```text
E2E scope: uniquely labeled synthetic expenses only; no pre-existing transaction will be changed.
Preflight passed: credential, sync, writable account, and two active categories.
New-receipt preview passed: 0.01 + 0.02 = 0.03 and no write occurred.
New-receipt apply passed: verified total and repeat-apply idempotency.
Reconciliation preview passed: 0.06 -> 0.02 + 0.03 = 0.05 and no write occurred.
Reconciliation apply passed: amount correction, split, categories, total, and idempotency verified.
Cleanup passed: 4/4 exact synthetic transaction IDs are absent or deleted.
Live write E2E passed. No pre-existing transaction was changed.
```

## Fresh Codex capability probe

A separate fresh Codex session (`01a00548-c4bf-7e33-9207-248b944c6966`) was restricted to read-only calls. It successfully connected and synchronized, discovered the upgraded contract, and returned:

```text
1. Yes — create a brand-new categorized receipt expense:
   zenmoney_preview_new_receipt -> zenmoney_apply_new_receipt

2. Yes — correct an existing expense total and split it into categorized parts:
   zenmoney_preview_receipt_reconciliation -> zenmoney_apply_receipt_reconciliation

Each apply requires the fresh previewToken and confirmed: true after explicit user acceptance.
I called no preview or mutation tools and changed no ZenMoney data.
```

## Result

Passed. Brand-new receipt creation, categorized allocation, existing-expense amount correction, splitting, exact-total verification, repeat-apply idempotency, and exact-ID cleanup were all confirmed against the live API.
